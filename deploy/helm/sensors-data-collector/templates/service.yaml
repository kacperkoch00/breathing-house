apiVersion: v1
kind: Service
metadata:
  name: {{ include "sensors-data-collector.fullname" . }}
  labels:
    {{- include "sensors-data-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sensors-data-collector.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
