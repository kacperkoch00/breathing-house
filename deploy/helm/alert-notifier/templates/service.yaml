apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-notifier.fullname" . }}
  labels:
    {{- include "alert-notifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "alert-notifier.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
