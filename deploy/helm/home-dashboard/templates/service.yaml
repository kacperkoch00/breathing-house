apiVersion: v1
kind: Service
metadata:
  name: {{ include "home-dashboard.fullname" . }}
  labels:
    {{- include "home-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "home-dashboard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
