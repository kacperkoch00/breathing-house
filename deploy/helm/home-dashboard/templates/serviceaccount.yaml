{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "home-dashboard.serviceAccountName" . }}
  labels:
    {{- include "home-dashboard.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
