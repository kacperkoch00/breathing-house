apiVersion: v1
kind: Service
metadata:
  name: {{ include "environment-monitor.fullname" . }}
  labels:
    {{- include "environment-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "environment-monitor.selectorLabels" . | nindent 4 }}
