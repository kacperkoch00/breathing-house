apiVersion: v1
kind: Service
metadata:
  name: {{ include "occupancy-monitor.fullname" . }}
  labels:
    {{- include "occupancy-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "occupancy-monitor.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
